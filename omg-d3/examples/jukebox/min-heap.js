module.exports = MinHeap;

function MinHeap(size) {
    this.heapSize = size;
    this.data     = new Array(size);
}

MinHeap.prototype.add = function(d) {
    
};

MinHeap.prototype.heapify = function() {

};

function Node(name, value) {
    this.name = name;
    this.value = value;
}

